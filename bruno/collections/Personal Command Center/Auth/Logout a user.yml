info:
  name: Logout a user
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/auth/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
