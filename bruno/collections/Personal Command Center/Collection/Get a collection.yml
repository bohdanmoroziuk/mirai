info:
  name: Get a collection
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/collections/{{COLLECTION_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
