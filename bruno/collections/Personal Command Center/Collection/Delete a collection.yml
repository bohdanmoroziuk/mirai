info:
  name: Delete a collection
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BASE_URL}}/collections/{{COLLECTION_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
