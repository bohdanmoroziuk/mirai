info:
  name: Update collection
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{BASE_URL}}/collections/{{COLLECTION_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
