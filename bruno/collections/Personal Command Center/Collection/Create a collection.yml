info:
  name: Create a collection
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/collections"
  body:
    type: json
    data: |-
      {
        "title": "Movies"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
