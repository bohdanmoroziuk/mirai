info:
  name: Update case
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{BASE_URL}}/cases/{{CASE_ID}}"
  body:
    type: json
    data: |-
      {
        "title": "Relaxation"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
