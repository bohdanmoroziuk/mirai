info:
  name: Delete case
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{BASE_URL}}/cases/{{CASE_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
