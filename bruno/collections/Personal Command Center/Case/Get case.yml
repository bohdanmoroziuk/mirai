info:
  name: Get case
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/cases/{{CASE_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
