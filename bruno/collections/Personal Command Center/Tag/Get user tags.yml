info:
  name: Get user tags
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/tags?search=act"
  params:
    - name: search
      value: act
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
