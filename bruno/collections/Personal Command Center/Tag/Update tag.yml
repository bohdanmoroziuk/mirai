info:
  name: Update tag
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{BASE_URL}}/tags/{{TAG_ID}}"
  body:
    type: json
    data: |-
      {
        "color": "#00DC82"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
