info:
  name: Delete tag
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BASE_URL}}/tags/{{TAG_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
