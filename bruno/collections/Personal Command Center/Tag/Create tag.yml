info:
  name: Create tag
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/tags"
  body:
    type: json
    data: |-
      {
        "name": "vue",
        "color": "#41B883"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
