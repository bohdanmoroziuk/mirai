info:
  name: Create tag
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/tags"
  body:
    type: json
    data: |-
      {
        "name": "react"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
