info:
  name: Get user bookmarks
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/bookmarks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
