info:
  name: Get bookmark
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/bookmarks/{{BOOKMARK_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
